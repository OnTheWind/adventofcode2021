
/**
 * Imports a file for use
 * @param path path to the file to import
 * @returns an array of the file with each line as a new element in the array
 */
export function fileToArray(path): string[] {
    return [];
}